/**
 * Generated by orval 🍺
 * Do not edit manually.
 * HR Agency System
 * REST API for managing recruitment agency operations.
 *
 * The API provides functionality for:
 * - organization and user management
 * - sales opportunities and activities
 * - companies and contacts
 * - job descriptions and job postings
 * - candidates and job applications
 * - schedule interviews
 * OpenAPI spec version: v1
 */
import type { FormKind } from "./formKind.ts";
import type { ResponseCardinality } from "./responseCardinality.ts";

export interface CreateFormRequest {
	/** Stable code, unique in the organization, e.g. gdpr-consent. Never changes. */
	code: string;
	/** What people read. */
	name: string;
	/** @nullable */
	description: string | null;
	/** Document or Survey - a label and a default, not a different mechanism. */
	kind: FormKind;
	cardinality: null | ResponseCardinality;
	/**
	 * Whose responses these are. Only 'worker' today.
	 * @nullable
	 */
	subjectKind: string | null;
}
