/*
 * These three turned out not to be about projects at all - an assignment has a period and a worker
 * has an address - so they live in `utlis` now. Re-exported here to keep the project screens'
 * imports where they were.
 */
export { formatAddress, formatPeriod, toDateOnly } from "#/utlis/formatRecord";
